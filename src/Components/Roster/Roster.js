import React from 'react';
//import promo1 from './images/promo1.png'

const Roster = (props) => { // renders products based on the 
    return ( <div>
        <h1>{props.title}</h1>

    </div> );
}
 
export default Roster;